# Core (Model A: TF-IDF + Logistic Regression, data prep, evaluation)
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
matplotlib>=3.8
joblib>=1.3

# Model B: LSTM  (CPU wheels: pip install torch --index-url https://download.pytorch.org/whl/cpu)
torch>=2.2

# Model C: BERT
transformers>=4.40
